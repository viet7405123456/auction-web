package com.example.auction_web.entity.Enumtype;

public enum DocumentType {
    REGISTRATION,      // Đăng ký xe
    INSPECTION,        // Đăng kiểm
    INSURANCE,         // Bảo hiểm
    AUTHORIZATION,     // Giấy ủy quyền (nếu có)
    INVOICE,           // Hóa đơn (nếu có)
    OTHER
}
